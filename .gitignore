# game data — never commit, copyright and size
original/

# build outputs — regenerable from tools/assemble.py build / tools/engine.py
build/*.BIN
build/*.EXE
build/SLPS_008.29
build/*.ext.txt

# python
__pycache__/
*.pyc

# subagent worktrees created by the /translate loop
.claude/worktrees/
